global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: verigate
    static_configs:
      # scrape the gateway running on the host (Docker Desktop / host-gateway)
      - targets: ['host.docker.internal:8000']
